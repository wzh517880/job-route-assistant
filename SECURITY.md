# 安全与隐私待审事项

适用版本：v0.3.1。当前为私有仓库中的待审预览版，未完成全面安全审核。本文件记录已知问题；没有把任何问题标记为已修复。

## 当前使用范围

暂勿公开分发或邀请他人使用真实资料测试。请仅在独立浏览器测试配置文件中使用虚构简历和虚构投递记录。不要向仓库、Issue、评论或附件上传真实简历、联系方式、身份证号、求职备份、通知截图、会话令牌或其他凭证。

## 已知：网页脚本可读取侧栏中的简历值

静态代码核查发现：

- `manifest.json` 的内容脚本匹配所有 HTTP / HTTPS 页面。
- `content.js` 使用 `host.attachShadow({ mode: 'open' })`，网页脚本可访问该节点的 `shadowRoot`。
- `fieldButton()` 把简历值写入 `data-fill-value`、`title` 和文本；`render()` 包含基本资料及当前选择的各段经历。
- 文件初始化时调用 `loadState()`，立即读取并渲染简历，侧栏是否展开不影响这些值存在于页面结构中。
- 版本和经历切换事件未验证是否由真实用户触发，页面可能通过模拟事件读取其他版本或经历。

这意味着资料虽然存储在浏览器本地，当前网页脚本仍可能在用户明确选择填写之前读到资料。仅隐藏面板、限制批量填写身份证字段或对字符串做 HTML 转义，不能解决此隔离问题。当前结论来自静态代码检查，尚未完成真实浏览器复现及修复后验证。

修复方向：将含简历资料的界面放在扩展自己的 sidePanel 或经过隔离设计的扩展页面中；网页只接收用户明确选择填写的值。检查消息来源、目标标签页和真实用户操作，避免页面通过模拟事件主动索取简历。完成后应在带有恶意脚本的本地测试页使用虚构资料验证：未操作、侧栏收起、切换版本和经历等场景均不能提前读取资料；正常单项及批量填写仍符合用户选择。

## 依赖许可与来源待核

`THIRD_PARTY_NOTICES.md` 为原始说明。OCR 文件引用的 `tesseract.min.js.LICENSE.txt` 和 `worker.min.js.LICENSE.txt` 缺失，完整许可证、编译依赖和打包依赖的归属声明尚待整理。OCR core 与中文模型缺少足以确定精确来源修订的记录。应在公开分发前完成版本和文件指纹核定，补齐适用的许可证及归属声明，并记录必要的修改说明。

本次私有仓库存档没有联网补齐依赖，也没有选择应用代码的开源许可证。记录这些缺口不表示许可要求已满足。

## 其他限制

- Windows Edge 悬浮球不显示的问题尚未定位，不能把 v0.3.1 视为已修复。
- OCR、PDF 草稿和自动填写可能识别有误，应使用虚构数据核对实际行为。
- 备份包含简历资料、岗位信息及留档原文件，可能携带全部个人求职资料；不要提交到仓库。导入会替换当前数据，卸载扩展或清除浏览器数据可能造成数据丢失。
- 尚未进行完整的依赖漏洞扫描、动态渗透测试或跨浏览器验证。

## 问题记录

在私有协作范围内记录最少必要的复现步骤、浏览器版本和虚构数据样例。涉及隐私暴露时，使用明确的虚构占位值证明问题即可，不附真实用户记录。当前未建立公开漏洞报告邮箱或响应时限承诺。
