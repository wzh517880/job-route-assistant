# 求职航线 v0.3.1 更新说明

## 本次修改

- “新增一行”和“添加公司”在表格中直接新增空白行，并定位到公司输入框，不再自动弹出档案。
- 公司名称可以直接点击编辑、自动保存；下方“查看详细信息”是独立按钮，通过间距和分隔线避免误触。
- 默认在“AI面试”前新增“待测评”。升级现有数据和导入旧备份时会补上该选项，不删除自定义状态。
- 管理状态支持拖动左侧手柄调整顺序，也提供上下按钮；手柄获得键盘焦点后可按上下方向键排序。
- 顺序自动保存，刷新后仍保留，同步到顶部航线、筛选器、岗位下拉框和进度时间线。排序不改变现有岗位的投递状态。
- 新版导出的 JSON 保留自定义顺序；仍支持旧版 JSON 和包含附件的完整备份。

## 已安装旧版时更新

先在旧版主看板导出 JSON 备份，确认文件已下载。不要先删除插件或清除浏览器数据。

如果希望原地更新：

1. 解压 v0.3.1 ZIP，打开里面直接包含 `manifest.json` 的文件夹。
2. 在扩展“详细信息”页面确认旧插件的加载位置，保留这个文件夹的名称和路径。
3. 用新版文件夹中的全部文件和子文件夹替换旧加载文件夹内的对应程序文件（不是再套一层文件夹）。
4. 在 `chrome://extensions/` 或 `edge://extensions/` 点击该插件的“重新加载”。
5. 确认版本显示 0.3.1，再刷新主看板和招聘网页；检查数据是否完整。始终保留第 1 步导出的备份。

如果选择单独安装新版：

1. 解压 ZIP 后关闭旧版插件开关，不要先卸载旧版。
2. 在扩展管理页面开启开发者模式，点击“加载已解压的扩展程序”。
3. 选择**直接包含 `manifest.json` 的文件夹**，不是 ZIP，也不是外层文件夹。
4. 确认版本为 0.3.1，打开新版主看板，按需导入先前备份。
5. 导入会替换而不是合并当前数据。新文件夹可能产生不同扩展 ID，旧数据不一定自动出现。
6. 安装后不要移动、重命名或删除加载的文件夹。

## 范围

这是桌面 Chrome / Edge 开发者模式插件。此次修复针对表格编辑与状态管理，不代表先前 Windows Edge 上悬浮球不显示的问题已经定位或修复。
